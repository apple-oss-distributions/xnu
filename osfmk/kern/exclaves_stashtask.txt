Send a message in #exclaves-xnu-integration describing this change (see pinned message).

If you are not already in #exclaves-xnu-integration: 
```
/msg @DisclosureBot request access to exclaves-xnu-integration
```
